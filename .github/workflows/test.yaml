name: test

run-name: aws s3 test

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read
  actions: read

jobs:
  test-upload:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create test file
        run: |
          mkdir -p test-artifacts
          echo "This is a test upload file." > test-artifacts/test.txt
          echo "✅ Created test file:"
          ls -l test-artifacts/

      - name: Upload to S3 (using custom action)
        uses: hanwhavision/.github/.github/actions/upload-apk-to-s3@JSMPFB-40
        with:
          file: test-artifacts/test.txt
          destination: s3://mopl015-qacommon-q-an2/apk
          role_arn: arn:aws:iam::339712975332:role/HV-platform-tf-automation
          aws_accountid: 103827304329
          app_name: guard
          version: 1.26.0(121)